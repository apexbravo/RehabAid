@page
@model RehabAid.Web.Areas.Facilities.Pages.Patients.IndexModel
@{
}
@section breadcrumb{
    <a class="breadcrumb-item" asp-page="Patients/Index">Patients </a>

}
@*
      @if (User.IsSysUser())
    {
    }

*@
<div class="btn-toolbar float-end d-inline pb-3">
    <a class="btn btn-sm btn-outline-secondary " asp-page="Add"><i class="fa fa-pluss"></i> Add new Patient</a>
</div>


<h4 class="font-weight-light text-primary"><strong>@Model.List.TotalItemCount</strong> Patient@(Model.List.TotalItemCount == 1 ? "" : "s") @(Model.QueryString != null ? "" : "found")</h4>
<table class="table table-striped table-hover table-sm">
    <thead>
        <tr>
            <th scope="col">First Name</th>
            <th scope="col">Surname</th>
            <th scope="col">Age</th>
            <th scope="col">Gender</th>
            <th scope="col">Emergency Contact</th>
            <th scope="col">Address</th>

        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.List)
        {
        <tr>

            @*<td>
                   <div>
                <div style="width: 45px; height: 45px; padding-top:10px !important" class="font-weight-bold text-center float-left mr-2 p-2 border border-dark rounded-circle bg-light">@($"{item.FirstName} {item.Surname}".Initials())</div>
                <div>

                </div>
            </div>

        </td>*@
            <td> <a asp-page="./Details" asp-route-id="@item.Id" class="p-3"> @item.FirstName</a></td>
            <td> @item.Surname</td>
            <td> @item.Age</td>
            <td>  @item.Gender</td>

            <td>
                @item.EmergencyContact
            </td>

            <td> 
                @item.Address
            </td>

        </tr>
        }
    <tfoot>

    </tfoot>

</table>
