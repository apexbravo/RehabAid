@page
@model RehabAid.Web.Areas.Facilities.Pages.Staffs.IndexModel
@{
}
@section breadcrumb{
    <a class="breadcrumb-item" asp-page="Staffs/Index">Staff </a>

}
@*
      @if (User.IsSysUser())
    {
    }

*@
<div class="btn-toolbar float-end d-inline pb-3">
    <a class="btn btn-sm btn-outline-secondary " asp-page="Add"><i class="fa fa-pluss"></i> Add new Staff</a>
</div>


<h4 class="font-weight-light text-primary"><strong>@Model.List.TotalItemCount</strong> staff@(Model.List.TotalItemCount == 1 ? "" : "") @(Model.QueryString != null ? "" : "found")</h4>
<table class="table table-striped table-hover table-sm">
    <thead>
        <tr>
            <th scope="col">First Name</th>
            <th scope="col">Surname</th>
            <th scope="col">Phone Number</th>
            <th scope="col">Email</th>

            <th scope="col">Role</th>
            <th scope="col">Address</th>

        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.List)
        {
        <tr>

            @*<td>
            <div>
                <div style="width: 45px; height: 45px; padding-top:10px !important" class="font-weight-bold text-center float-left mr-2 p-2 border border-dark rounded-circle bg-light">@($"{item.Name}".Initials())</div>
                <div>

                </div>
            </div>

        </td>*@
            <td> <a asp-page="./Details" asp-route-id="@item.Id" class="p-3"> @item.FirstName</a></td>

            <td> @item.Surname</td>
            <td>
                @item.PhoneNumber
            </td>
            <td>@item.Email</td>
            <td>
                @item.RoleId
            </td>
            
            <td>@item.Address</td>
            @*<td>
            @if (item.SpecialtyId == (int)SpecialtyType.Nurse_Practitioner)
            {
                <h4 class="badge bg-success d-inline">Nurse Practitioner</h4>
            }
            else if (item.SpecialtyId == (int)SpecialtyType.Medical_Doctor)

            {

                <h4 class="badge bg-warning d-inline text-black">Medical Doctor</h4>
            }
            else if (item.SpecialtyId == (int)SpecialtyType.Social_worker)

            {

                <h4 class="badge bg-warning d-inline text-black">Social Worker</h4>
            }
            else if (item.SpecialtyId == (int)SpecialtyType.Counselor)

            {

                <h4 class="badge bg-warning d-inline text-black">Counselor</h4>
            }
            else
            {
                <h4 class="badge bg-danger d-inline ">Psychiatrist</h4>
            }
        </td>*@

        </tr>
        }
    <tfoot>

    </tfoot>

</table>